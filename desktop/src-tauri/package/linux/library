#!/usr/bin/env sh
set -eu

# CLI entry point for Linux desktop packages. It runs the Python package
# embedded in the Tauri resources, so users do not need system Python.

if [ -z "${LIBRARY_HOME:-}" ]; then
  export LIBRARY_HOME="${HOME}/LibraryData"
fi

find_backend() {
  if [ -n "${LIBRARY_BUNDLED_BACKEND:-}" ] && [ -d "${LIBRARY_BUNDLED_BACKEND}" ]; then
    printf '%s\n' "${LIBRARY_BUNDLED_BACKEND}"
    return 0
  fi

  for dir in \
    "${APPDIR:-}/usr/lib/library-tauri/resources/backend" \
    "${APPDIR:-}/usr/lib/library/resources/backend" \
    "${APPDIR:-}/usr/lib/Library/resources/backend" \
    "${APPDIR:-}/resources/backend" \
    "/usr/lib/library-tauri/resources/backend" \
    "/usr/lib/library/resources/backend" \
    "/usr/lib/Library/resources/backend" \
    "/opt/Library/resources/backend"
  do
    if [ -n "${dir}" ] && [ -d "${dir}" ]; then
      printf '%s\n' "${dir}"
      return 0
    fi
  done

  return 1
}

backend="$(find_backend)" || {
  echo "library: cannot find bundled backend runtime" >&2
  exit 127
}

python=""
for candidate in "${backend}/bin/python3" "${backend}/bin/python" "${backend}/python"; do
  if [ -x "${candidate}" ]; then
    python="${candidate}"
    break
  fi
done

if [ -z "${python}" ]; then
  echo "library: cannot find bundled Python under ${backend}" >&2
  exit 127
fi

if [ -z "${PYTHONHOME:-}" ]; then
  export PYTHONHOME="${backend}"
fi
export PYTHONUNBUFFERED="${PYTHONUNBUFFERED:-1}"

module="${LIBRARY_PY_MODULE:-library.cli.repl}"
exec "${python}" -m "${module}" "$@"
